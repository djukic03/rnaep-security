server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
    location ~* \.(js|css)$ {
        root /usr/share/nginx/html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
}